[English](SECURITY.md) | [中文](SECURITY.zh-CN.md)

# 安全策略

> **版本**: v2.1
> 
> **最后更新**: 2026-08-02
>
> **编写**: [Little Tree Studio](https://github.com/Little-Tree-Studio) & [SRON 团队](https://github.com/SRON-org/)
> 
> **适用范围**: 所有实现 APICORE v2.x 协议的解析器、客户端、社区 Hub 及配置文件贡献者

## 目录

- [安全边界](#安全边界)
- [安全控制与标志](#安全控制与标志)
- [安全集成 `run`](#安全集成-run)
- [不可信输入与资源限制](#不可信输入与资源限制)
- [解码器与依赖安全](#解码器与依赖安全)
- [依赖漏洞策略](#依赖漏洞策略)
- [报告漏洞](#报告漏洞)

## 安全边界

APICORE_Python 是一个解析器和验证器。公共的 `load()`、`loads()`、`parse()` 和
`validate()` API 解码配置数据并返回类型化模型。它们不会发起 HTTP 请求、打开
浏览器链接或执行处理程序操作。

特别地，APICORE 的 `run` 处理程序被视为不可信数据：

- 解析器验证 `run` 处理程序具有非空的 `script` 字段。
- 脚本通过 `HandlerRule.script` 暴露；此 SDK 绝不会将其传递给 shell、解释器、
  `eval()` 或 `exec()`。
- 仓库 CLI 仅验证文档。
- 仓库 GUI 显示脚本并将其标记为高风险，但不会执行它。

由于此 SDK 不包含执行引擎，因此没有可启用或禁用的执行沙箱设置。成功解析文档
并不构成执行该文档中任何操作的授权。

## 安全控制与标志

APICORE_Python 刻意不提供 `allow_run`、`execute_handlers` 或 `sandbox_mode` 选项。
处理程序执行在 SDK 之外，因此添加 `allow_run=False` 标志会创建误导性的安全边界：
更改该标志无法保护宿主应用程序实现的代码。安全 SDK 行为是无条件的：

- `run` 执行始终不可用。
- 网络请求和 `browser` 操作始终不可用。
- YAML 始终使用包配置的安全加载器。
- 仓库 GUI 始终遮蔽标记为 `text_secret` 的参数。

添加执行层的应用程序应使用安全默认值定义自己的控制，例如 `allow_run=False`、
`require_run_approval=True` 和操作系统强制执行的沙箱策略。这些示例是宿主应用
程序控制，而非 APICORE_Python 构造函数或函数参数。当无法建立隔离时，沙箱标志
必须失败关闭；绝不能静默回退到在主应用程序进程中运行命令。

## 安全集成 `run`

自行实现 `run` 的应用程序承担安全边界，必须将每个脚本视为攻击者控制的，
包括来自本地文件的脚本。安全的宿主集成至少应：

1. 默认禁用执行，并要求对每个脚本或对狭义定义的可信策略进行明确的、知情的
   用户批准。
2. 优先使用结构化操作的固定允许列表，而非任意命令字符串。在与允许列表比较
   之前解析可执行文件路径。
3. 使用参数数组并禁用 shell 处理来调用已批准的程序，例如
   `subprocess.run(argv, shell=False, ...)`。不要对配置数据使用 `os.system()`、
   `shell=True`、`eval()` 或 `exec()`。
4. 拒绝 shell 控制语法，包括命令分隔符、管道、重定向、命令替换、环境变量
   展开和内嵌换行符。仅靠拒绝列表不足以构成充分的授权策略。
5. 阻止权限提升、系统管理、破坏性文件系统、凭据访问和任意下载器/解释器操作，
   除非狭义的产品需求明确允许其中之一。
6. 在操作系统强制执行的沙箱或隔离的工作进程中运行已批准的操作，使用专用的
   低权限身份、空或允许列表环境、无继承凭据、受限的工作目录，并在可能的情况
   下使用只读文件系统访问。
7. 默认拒绝网络访问，仅允许所需的目标地址。对 APICORE URL 和浏览器操作应用
   相同的 SSRF 防护。
8. 强制执行时间、内存、进程、文件和输出限制。在超时或取消发生时终止整个进程树。
9. 在审计日志中记录配置来源、策略决策、可执行文件、参数、退出状态和资源限制
   事件。从脚本、参数、环境变量和输出中编辑掉密钥。

仅靠容器并非完整的沙箱。使用操作系统的隔离控制，并将沙箱运行时和策略与持有
用户数据或凭据的应用程序进程分开。

## 不可信输入与资源限制

`load()` 将整个输入文件读入内存，解码器在解析嵌套数据时可能会分配额外的内存。
SDK 目前不强制文件大小、嵌套深度或解析时间限制。接受不可信输入的宿主应在调用
SDK 之前强制执行这些限制，并在拒绝服务成为关注点时在资源受限的工作进程中解析。

配置验证仅建立结构正确性。它不建立文档的身份、完整性或可信性。当来源重要时，
应通过经过身份验证的传输获取远程配置，并验证签名或可信摘要。不要直接在 APICORE
文档中存储凭据。

## 解码器与依赖安全

本包使用 `orjson` 处理 JSON，`msgspec` 处理 TOML，`ruamel.yaml` 处理 YAML。
YAML 通过 `YAML(typ="safe")` 实例化，避免从 YAML 标签构造任意 Python 对象。
不要将其替换为不安全或往返加载器来处理不可信文档。

APICORE_Python 不依赖 PyYAML。单独对不可信输入使用 PyYAML 的应用程序必须使用
`yaml.safe_load()` 或 `SafeLoader`，绝不能使用默认或不安全加载器的 `yaml.load()`。
安全对象构造不能防止资源耗尽攻击，因此仍然需要输入限制。

保持 `orjson`、`msgspec`、`ruamel.yaml` 和 `ruamel.yaml.clib` 为最新版本，
为部署保留已审查的锁文件，并扫描已解析的依赖项以查找已知漏洞。在发布前审查
依赖项更新，而不是仅依赖 `pyproject.toml` 中声明的最低版本。

### 依赖漏洞策略

- 直接和传递依赖的安全报告将根据 `uv.lock` 中解析的版本进行评估，而非仅根据
  `pyproject.toml` 中的版本范围。
- 受影响的依赖项应升级到最小的已审查、无漏洞的兼容版本。当直接依赖约束变更时，
  锁文件和包元数据必须同时更新。
- 依赖安全更新必须在发布前通过 Ruff、完整测试套件、包构建检查以及所有受影响
  格式和受支持 Python 版本的测试。
- 如果没有兼容的修复依赖项，维护者应移除或禁用受影响路径，应用狭义范围的缓解
  措施，或记录当前没有可用的安全版本。不得仅因为漏洞功能不常见就将有漏洞的
  版本描述为安全。
- 发布说明应标识受影响的包和修复版本，而不在用户有合理机会更新之前发布漏洞
  利用细节。

## 报告漏洞

仅通过电子邮件向 `admin@sr-studio.cn` 报告解析器行为、仓库工具、打包或依赖
处理中的漏洞，主题为 `[APICORE_Python Security]`。不接受其他提交渠道。

不要提交包含未修补漏洞、概念验证、敏感配置或零日详情的公开 Issue 或 Pull
Request。这包括但不限于不安全的反序列化、解析器资源耗尽、原生解码依赖中的
内存安全缺陷、仓库工具引入的 SSRF、命令执行、路径遍历、密钥泄露和授权绕过。

请在可用时提供以下信息：

- 受影响的 APICORE_Python、Python、操作系统和依赖项版本。
- 最小复现或概念验证以及所需的输入格式。
- 预期和观察到的行为、影响以及现实的攻击前提条件。
- 漏洞利用是否已知为公开或活跃的。
- 建议的缓解措施以及维护者安全联系您的方式。

维护者应私下确认收到，验证严重性和受影响版本，准备测试和修复，并在披露前
协调发布。报告者应给予维护者合理的修复期限，并在修复版本或双方同意的披露
日期可用之前避免分享漏洞利用细节。在用户可以更新后，维护者可以发布 GitHub
安全公告，并在被请求时致谢报告者。切勿在报告中包含密钥、个人数据、生产凭据
或从您不拥有的系统复制的数据。