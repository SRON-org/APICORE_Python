# Changelog

## 0.1.0

- Initial public release of `APICORE`.
- Fast JSON, YAML, and TOML parsing built on `msgspec`.
- APICORE v1 and v2 validation with v2 as the default when version is omitted.
- CLI validator via `apicore-validate`.
- Test suite and benchmark script for local verification.